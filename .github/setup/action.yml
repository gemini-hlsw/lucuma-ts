name: 'Setup CI Environment'

description: 'Sets up Node.js and pnpm for CI workflows'

inputs:
  install-deps:
    description: 'Whether to run pnpm install'
    required: false
    default: 'true'
  only-changed:
    description: 'Only run pnpm install on changed packages'
    required: false
    default: 'false'
  FONTAWESOME_NPM_AUTH_TOKEN:
    description: 'Fontawesome auth token (required if install-deps is true)'
    required: false

runs:
  using: 'composite'
  steps:
    - run: git fetch origin main:refs/remotes/origin/main --depth=1
      if: ${{ inputs.install-deps == 'true' && inputs.only-changed == 'true' }}
      shell: bash
    - uses: pnpm/action-setup@v6
    - name: Use Node.js
      uses: actions/setup-node@v6
      if: ${{ inputs.install-deps == 'true' }}
      with:
        node-version-file: '.nvmrc'
        cache: 'pnpm'
        registry-url: 'https://registry.npmjs.org/'
    - run: pnpm config set "//npm.fontawesome.com/:_authToken" "${{ inputs.FONTAWESOME_NPM_AUTH_TOKEN }}"
      if: ${{ inputs.install-deps == 'true' }}
      shell: bash
    - run: pnpm ci --prefer-offline
      if: ${{ inputs.install-deps == 'true' && inputs.only-changed == 'false' }}
      shell: bash
    - run: pnpm --filter "...[origin/main]..." ci --prefer-offline
      if: ${{ inputs.install-deps == 'true' && inputs.only-changed == 'true' }}
      shell: bash
